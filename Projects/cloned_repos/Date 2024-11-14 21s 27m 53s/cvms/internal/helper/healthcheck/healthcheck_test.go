package healthcheck

import (
	"testing"
)

func TestHealthCheck(t *testing.T) {
	// client := &http.Client{
	// 	Timeout: 5 * time.Second,
	// }

	// for _, endpoint := range endpoints {
	// 	fmt.Printf("Checking RPC URL: %s\n", endpoint.RPCURL)
	// 	checkURL(client, endpoint.RPCURL)

	// 	fmt.Printf("Checking LCD URL: %s\n", endpoint.LCDURL)
	// 	checkURL(client, endpoint.LCDURL)
	// }
}
